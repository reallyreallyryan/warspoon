---
interface Props {
  tag: string;
}

const { tag } = Astro.props;
---

<a
  href={`/tags/${tag}`}
  class="inline-block rounded-full bg-(--color-surface-700) px-3 py-1 text-xs font-medium text-(--color-accent) hover:bg-(--color-surface-800) transition-colors"
>
  {tag}
</a>
